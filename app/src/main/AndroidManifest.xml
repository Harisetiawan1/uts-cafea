<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="lat.sttg.informatika.UAS_WISATA">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="lat.sttg.informatika.UAS_WISATA.login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="lat.sttg.informatika.UAS_WISATA.MainActivity"/>
        <activity android:name="lat.sttg.informatika.UAS_WISATA.Bagendit"/>
        <activity android:name="lat.sttg.informatika.UAS_WISATA.Papandaian"/>
        <activity android:name="lat.sttg.informatika.UAS_WISATA.Santolo"/>
        <activity android:name="lat.sttg.informatika.UAS_WISATA.Darajat"/>
        <activity android:name="lat.sttg.informatika.UAS_WISATA.Talagabodas"/>
        </application>

</manifest>